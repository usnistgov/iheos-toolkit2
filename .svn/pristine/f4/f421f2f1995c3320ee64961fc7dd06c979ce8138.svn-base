package gov.nist.toolkit.xdstools2.client.tabs.directRegistrationTab;

import com.google.gwt.event.dom.client.ChangeEvent;
import com.google.gwt.event.dom.client.ChangeHandler;

public class UpdateDirectSelectionChangeHandler implements ChangeHandler {
	DirectRegistrationTab tab;
	
	public UpdateDirectSelectionChangeHandler(DirectRegistrationTab tab) {
		this.tab = tab;
	}
	
	@Override
	public void onChange(ChangeEvent arg0) {
//		tab.directExistingFromMessage("Loaded");
//		String selectedDirect = tab.currentDirectForUpdating();
//		if (selectedDirect != null && !selectedDirect.equals(""))
//			tab.cert.setText(new String(tab.currentRegistration.directToCertMap.get(selectedDirect)));
//		else
//			tab.cert.setText("");
	}

}
