package gov.nist.toolkit.xdstools2.client.tabs.directRegistrationTab;

import com.google.gwt.event.dom.client.ClickEvent;
import com.google.gwt.event.dom.client.ClickHandler;

public class SelectDirect implements ClickHandler {
	DirectRegistrationTab tab;
	
	public SelectDirect(DirectRegistrationTab tab) {
		this.tab = tab;
	}

	@Override
	public void onClick(ClickEvent arg0) {
//		String selectedDirectAddr = tab.currentDirect();
//		if (selectedDirectAddr == null)
//			return;
//		String cert = tab.currentRegistration.directToCertMap.get(selectedDirectAddr);
//		tab.cert.setText(cert);
	}

}
