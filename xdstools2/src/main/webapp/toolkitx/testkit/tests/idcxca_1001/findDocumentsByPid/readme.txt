<h3>Send Registry Stored Query (ITI-XXX) Request to Initiating Gateway.</h3>

<p/>When the test section is run:
<ol>
<li/>The Image Document Consumer SUT will send a Registry Stored
Query (by PID) (ITI-XXX Request) to the Initiating Gateway System, 
requesting documents for this patient.

