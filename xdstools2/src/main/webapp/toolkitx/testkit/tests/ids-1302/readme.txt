Multi-image retrieve

<h2>Retrieve Image Document Set, Multi-Image study</h2>

<h2>Retrieve Multi Image Study</h2>

<p>When the test is run:
<ol>
<li/>The Image Document Consumer simulator (IDC sim) sends a Retrieve Image 
Document Set Request (RAD-69 Request) to the Imaging Document Source System
Under Test (IDS SUT).
<li/>The IDS SUT is expected to process the test and generate a response
containing the requested image.
<li/>The IDC sim will process the RAD-69 response and store the received
image.
</ol>
</p>

<h2>Validate SOAP Response and returned DICOM Image file</h2>

<p>
The following validations are performed:
<ol>
<li>Returned image matches that requested.
<li>Correct format of the RAD-69 response generated by the IDS SUT.
</ol>
</p>